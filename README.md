# React_campar_leaderboard

Freecodecamp's React Campar Leaderboard

# How to install:

Clone repo or download file. Open terminal or cmd and paste this command:

```bash
cd campar_leaderboard && npm install && npm start
```

**Enjoy**
